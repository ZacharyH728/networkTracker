FROM python:3.12-slim

WORKDIR /app

COPY requirements.api.txt .
RUN pip install --no-cache-dir -r requirements.api.txt

COPY network_tracker/ ./network_tracker/
COPY config.ini .

RUN mkdir -p data

CMD ["uvicorn", "network_tracker.api:app", "--host", "0.0.0.0", "--port", "8000"]
